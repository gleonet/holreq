<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? "HolReq - " + yield(:title) : "HolReq" %></title>

    <%= stylesheet_link_tag    "application", media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "application", 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <div class="title-bar">
        <div class="title-bar-left">
          <button class="menu-icon" type="button" data-toggle="menu"></button>
          <div class="title-bar-title"><strong data-toggle="menu">HolReq</strong></div>
        </div>
      </div>
      <div class="top-bar hide" id="menu" data-toggler=".hide">
        <div class="top-bar-left">
          <ul class="dropdown menu" data-dropdown-menu>
            <li><a href="#"><i class='fi-home'></i> Home</a></li>
            <li><a href="#"><i class='fi-calendar'></i> Calendar</a></li>
            <li><a href="#"><i class='fi-widget'></i> Admin</a></li>
          </ul>
        </div>
      </div>
    </header>

    <main>
    <div class='expanded row'>
      <% if !flash.empty? -%>
        <% flash.each do |type, message| -%>
          <div class="<%= type %> callout">
            <p><%= message %></p>
          </div>
        <% end -%>
      <% end -%>
      <% if content_for?(:right) -%>
        <div class='large-9 columns'>
          <fieldset class='fieldset'>
            <%= yield %>
          </fieldset>
        </div>
        <div class='large-3 columns' id='right-column'>
          <%= yield(:right) %>
        </div>
      <% else -%>
        <div class='large-12 columns'>
          <fieldset class='fieldset'>
            <%= yield %>
          </fieldset>
        </div>
      <% end -%>
    </div>
    </main>

  </body>
</html>
