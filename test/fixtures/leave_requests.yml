# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  user_id: users(:one).id
  leave_id: leaves(:one).id
  leave_type_id: leave_types(:one).id
  start_date: <%= 2.days.since.to_datetime %>
  end_date: <%= 3.days.since.to_datetime %>
  status: submitted
  approved_by_id: users(:one).id
  nb_hours: 16
  description: Test
  range: range_date
  period: all

two:
  user_id: users(:one).id
  leave_id: leaves(:one).id
  leave_type_id: leave_types(:one).id
  start_date: <%= 2.days.since.to_datetime %>
  end_date: <%= 3.days.since.to_datetime %>
  status: approved
  approved_by_id: users(:one).id
  nb_hours: 8
  description: MyString
  range: range_date
  period: all
