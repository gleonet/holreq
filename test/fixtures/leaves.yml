# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  user_id: users(:one).id
  year: 2016
  leave_type_id: leave_types(:one).id
  nb_hours: 40

two:
  user_id: users(:one).id
  year: 2016
  leave_type_id: leave_types(:two).id
  nb_hours: 50
